/**
 * Deploys all contracts and check their health
 */

const deploySystem = async () => {
  // const quoteAsset = await deployToken();
};

if (require.main === module) {
  // compile("exchanges/MatchingMarket.sol");
  deploySystem();
}

export default deploySystem;
