import config from './config.json';

export const mainnet = {
  chai: config.chai,
  chaiPriceSource: config.chaiPriceSource,
  chainlinkPriceSources: config.chainlinkPriceSources,
  kyber: config.kyber,
  tokens: config.tokens,
  whales: config.whales,
};
