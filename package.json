{
  "private": true,
  "workspaces": ["persistent", "release", "utils"],
  "scripts": {
    "postinstall": "lerna bootstrap && lerna link",
    "build": "lerna run build",
    "test": "lerna run test",
    "format": "lerna run format",
    "lint": "lerna run lint",
    "typecheck": "lerna run typecheck",
    "clean": "lerna run clean"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
